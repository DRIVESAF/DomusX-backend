<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.gx.dao.TUserTenantDao">

    <insert id="insertUserTenant" parameterType="map">
        INSERT INTO t_user_tenant (user_id, tenant_id, create_time, update_time, deleted)
        VALUES (#{userId}, #{tenantId}, NOW(), NOW(), 0)
    </insert>

</mapper>